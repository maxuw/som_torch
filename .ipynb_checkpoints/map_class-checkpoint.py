import torch

class Map:

    def dupa(self):
        print("dupa")


class Map:

    def __init__(self, length, width, node_dimenstion):
        print("dupa")
    #     self.length = length
    #     self.width = width
    #     self.node_dimenstion = node_dimenstion
    #
    #     # I'm defining
    #
    #     self.map = initialize_map(self.length, self.width, self.node_dimenstion)
    #
    #     # self.initialize_location(self.length, self.width, self.node_dimenstion)
    #
    # def initialize_map(self, length, width, dimention):
    #     map_init = torch.rand((dimention, length, width))
    #     return map_init
